;; Additional exercises

;; Create a namespace and swtich to it.

;; Use `refer`.

;; Use `ns-interns`

;; Use `:only`, `:exclude`, and `:rename`

;; Use a private function in a namespace. Why might you want to use a private
;; function.

;; What is (min [1 5 0]) - why doesn't it do what you "want"? Can you fix it with
;; `apply`?

;; Use `->>` in a function.
